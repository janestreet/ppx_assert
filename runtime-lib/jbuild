(library
  ((name ppx_assert_lib)
   (public_name ppx_assert.runtime-lib)
   (libraries (base base.caml sexplib))
   (preprocess (pps (ppx_type_conv ppx_sexp_conv)))
   (js_of_ocaml ())))

(jbuild_version jane_street)
